export declare class Stash {
    private stash;
    constructor();
    push(log: string, { extra }?: {
        extra?: boolean;
    }): void;
    toString({ extra }?: {
        extra?: boolean;
    }): string;
    toCLIString(): string;
    render(): void;
}
export declare function makeTitle(color: any, title: string, ...rest: string[]): string;
